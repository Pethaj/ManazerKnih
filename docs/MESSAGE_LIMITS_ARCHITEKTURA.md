# 🏗️ Architektura systému denních limitů zpráv

## 📊 Přehled architektury

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER ACTION                              │
│                    (odešle zprávu do chatu)                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      CHAT API ENDPOINT                           │
│                   (Next.js /api/chat/send)                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 1. Zavolá checkMessageLimit()
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   EDGE FUNCTION: check-message-limit             │
│  https://modopafybeslbcqjxsve.supabase.co/functions/v1/...     │
├─────────────────────────────────────────────────────────────────┤
│  Action: "check"                                                 │
│  ┌──────────────────────────────────────────────────┐          │
│  │ 1. Zkontroluj reset_at (je čas na reset?)       │          │
│  │ 2. Načti globální limit                         │          │
│  │ 3. Zkontroluj: global.current < global.limit?   │          │
│  │ 4. Načti chatbot limit                          │          │
│  │ 5. Zkontroluj: chatbot.current < chatbot.limit? │          │
│  └──────────────────────────────────────────────────┘          │
│                                                                  │
│  Vrací: { allowed: true/false, reason?, message? }              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                ┌────────────┴──────────────┐
                │                           │
         limit OK?                   limit exceeded?
                │                           │
                ▼                           ▼
     ┌──────────────────────┐    ┌──────────────────────┐
     │ 2. Odešli zprávu     │    │ Vrať 429 Too Many    │
     │    do AI modelu      │    │ Requests + hlášku    │
     └──────┬───────────────┘    └──────────────────────┘
            │                              │
            ▼                              ▼
     ┌──────────────────────┐    ┌──────────────────────┐
     │ 3. Ulož do DB        │    │ Zobraz pěknou        │
     └──────┬───────────────┘    │ hlášku v UI          │
            │                     └──────────────────────┘
            ▼
     ┌──────────────────────────────────────────────┐
     │ 4. Zavolá incrementMessageCount()            │
     └──────┬───────────────────────────────────────┘
            │
            ▼
     ┌─────────────────────────────────────────────────────┐
     │     EDGE FUNCTION: check-message-limit              │
     │     Action: "increment"                             │
     ├─────────────────────────────────────────────────────┤
     │  1. Zavolej increment_message_count() pro global    │
     │  2. Zavolej increment_message_count() pro chatbot   │
     └─────────────────────────────────────────────────────┘
            │
            ▼
     ┌─────────────────────────────────────────────────────┐
     │           DATABASE: message_limits                   │
     │  ┌────────────────────────────────────────────────┐ │
     │  │ UPDATE message_limits                          │ │
     │  │ SET current_count = current_count + 1          │ │
     │  │ WHERE id = ...                                 │ │
     │  └────────────────────────────────────────────────┘ │
     └─────────────────────────────────────────────────────┘
```

---

## 🕐 Denní reset flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    KAŽDÝ DEN V 00:05 CET                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   CRON JOB (pg_cron)                            │
│  SELECT cron.schedule('reset-message-limits-daily', ...)       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP POST
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│           EDGE FUNCTION: reset-message-limits-cron              │
│  https://modopafybeslbcqjxsve.supabase.co/functions/v1/...     │
├─────────────────────────────────────────────────────────────────┤
│  1. Ověří Authorization (Service Role Key)                      │
│  2. Zavolá: SELECT reset_all_message_limits()                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│           DATABASE FUNCTION: reset_all_message_limits()         │
├─────────────────────────────────────────────────────────────────┤
│  UPDATE message_limits                                          │
│  SET                                                             │
│    current_count = 0,                                           │
│    reset_at = next_midnight_CET,                                │
│    updated_at = NOW()                                           │
│  WHERE reset_at <= NOW()                                        │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼
                    ✅ Všechny čítače = 0
```

---

## 💾 Databázová struktura

```sql
┌─────────────────────────────────────────────────────────┐
│                   message_limits                         │
├─────────────────┬─────────────┬─────────────────────────┤
│ id              │ UUID        │ PRIMARY KEY             │
│ chatbot_id      │ TEXT        │ NULL = globální limit   │
│ daily_limit     │ INTEGER     │ NULL = bez limitu       │
│ current_count   │ INTEGER     │ Aktuální počet dnes     │
│ reset_at        │ TIMESTAMPTZ │ Čas příštího resetu     │
│ created_at      │ TIMESTAMPTZ │                         │
│ updated_at      │ TIMESTAMPTZ │                         │
└─────────────────┴─────────────┴─────────────────────────┘
                         │
                         │ UNIQUE(chatbot_id)
                         │
        ┌────────────────┴────────────────┐
        │                                 │
        ▼                                 ▼
┌───────────────────┐          ┌───────────────────┐
│ GLOBÁLNÍ LIMIT    │          │ CHATBOT LIMIT     │
├───────────────────┤          ├───────────────────┤
│ chatbot_id: NULL  │          │ chatbot_id: "xyz" │
│ daily_limit: 100k │          │ daily_limit: 5000 │
│ current_count: 0  │          │ current_count: 0  │
└───────────────────┘          └───────────────────┘
```

---

## 🔄 Datový tok

### 1. Kontrola limitu (check)

```
User → Chat API → checkMessageLimit()
                       ↓
          Edge Function: check-message-limit
                       ↓
           ┌───────────┴───────────┐
           ▼                       ▼
    SELECT global_limit    SELECT chatbot_limit
           │                       │
           └───────────┬───────────┘
                       ▼
                  KONTROLA:
           current_count >= daily_limit?
                       │
           ┌───────────┴───────────┐
           ▼                       ▼
      limit OK               limit exceeded
           │                       │
           └───────────┬───────────┘
                       ▼
                 RESPONSE
```

### 2. Inkrementace (increment)

```
User message sent → incrementMessageCount()
                           ↓
              Edge Function: check-message-limit
                           ↓
           ┌───────────────┴────────────────┐
           ▼                                ▼
increment_message_count()      increment_message_count()
    (global limit)                  (chatbot limit)
           │                                │
           ▼                                ▼
   global.current++               chatbot.current++
```

---

## 🏛️ Hierarchie limitů

```
┌─────────────────────────────────────────────────┐
│           GLOBÁLNÍ LIMIT (nadřazený)            │
│                 100,000 / den                    │
│              current: 45,230                     │
└────────────────────┬────────────────────────────┘
                     │
       ┌─────────────┼─────────────┐
       ▼             ▼             ▼
┌───────────┐ ┌───────────┐ ┌───────────┐
│ Chatbot A │ │ Chatbot B │ │ Chatbot C │
│  5k / den │ │ 10k / den │ │  bez lim. │
│ curr: 2.3k│ │ curr: 8.9k│ │ curr: 1.2k│
└───────────┘ └───────────┘ └───────────┘

Pravidlo:
- Pokud GLOBÁLNÍ dosáhne 100k → VŠECHNY chatboty zastaví
- Pokud Chatbot A dosáhne 5k → pouze Chatbot A zastaví
- Globální limit má přednost
```

---

## 🔐 Bezpečnost

```
┌─────────────────────────────────────────────┐
│         RLS (Row Level Security)            │
├─────────────────────────────────────────────┤
│ Admin:                                      │
│   - Plný přístup ke všem limitům            │
│                                             │
│ Authenticated users:                        │
│   - Čtení: své chatboty + globální limit    │
│   - Zápis: pouze přes Edge Function         │
│                                             │
│ Service role:                               │
│   - Plný přístup (pro Edge Functions)       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│         Edge Function Authorization         │
├─────────────────────────────────────────────┤
│ check-message-limit:                        │
│   - verify_jwt: false                       │
│   - Vyžaduje: SUPABASE_ANON_KEY             │
│                                             │
│ reset-message-limits-cron:                  │
│   - verify_jwt: false                       │
│   - Vyžaduje: SUPABASE_SERVICE_ROLE_KEY     │
└─────────────────────────────────────────────┘
```

---

## ⚡ Performance

### Optimalizace

1. **Indexy:**
   - `idx_message_limits_chatbot` - rychlé vyhledání podle chatbot_id
   - `idx_message_limits_reset` - rychlá kontrola reset_at

2. **Atomické operace:**
   - `increment_message_count()` používá SQL UPDATE
   - Thread-safe, bez race conditions

3. **Caching:**
   - Edge Functions běží na edge (nízká latence)
   - Databázové funkce jsou SECURITY DEFINER (cached plans)

4. **Fail-open strategie:**
   - Pokud kontrola limitu selže → zpráva projde
   - Lepší UX než False negative

---

## 📊 Monitoring bod

```
┌─────────────────────────────────────────────┐
│           Supabase Dashboard                │
├─────────────────────────────────────────────┤
│ Edge Functions → Logs:                      │
│   - Počet volání check-message-limit        │
│   - Počet zamítnutých zpráv                 │
│   - Chybovost                               │
│                                             │
│ Database → Table Editor:                    │
│   - Aktuální stav všech limitů              │
│   - Využití v %                             │
│   - Čas do resetu                           │
│                                             │
│ Cron Jobs → pg_cron:                        │
│   - Historie běhů                           │
│   - Success/failure rate                    │
└─────────────────────────────────────────────┘
```

---

## 🔍 Troubleshooting flow

```
Problem: Zprávy procházejí i když je limit dosažen
    ↓
1. Zkontroluj Edge Function logy
    ↓
2. SELECT * FROM message_limits WHERE chatbot_id = 'xxx'
    ↓
3. Ověř že current_count se zvyšuje
    ↓
4. Zkontroluj API integraci (volá checkMessageLimit?)
    ↓
5. Testuj manuálně curl requestem
```

```
Problem: Čítače se neresetují
    ↓
1. SELECT * FROM cron.job WHERE jobname = 'reset-message-limits-daily'
    ↓
2. SELECT * FROM cron.job_run_details (historie)
    ↓
3. Zkontroluj Edge Function logy (reset-message-limits-cron)
    ↓
4. Testuj manuálně: curl POST reset endpoint
    ↓
5. Zkontroluj timezone nastavení
```

---

## 🎯 Design decisions

### Proč samostatná tabulka message_limits?

✅ **PRO:**
- Čisté oddělení runtime dat od konfigurace
- Rychlé čtení (malá tabulka)
- Snadný reset bez dotýkání chat_messages
- Možnost sledovat historii

❌ **PROTI:**
- Extra tabulka (ale malá)

### Proč Edge Functions místo API Routes?

✅ **PRO:**
- Běží na edge (nižší latence)
- Auto-scaling od Supabase
- Separace concerns
- Možnost volat z různých míst

### Proč 1 konverzační dvojice = 1 zpráva?

✅ **PRO:**
- Odpovídá tomu kde vznikají náklady (AI odpověď)
- Spravedlivější než počítat každou zprávu zvlášť
- Jednodušší logika pro uživatele

---

**Vytvořeno:** 2026-01-30  
**Účel:** Vizuální dokumentace architektury systému limitů
